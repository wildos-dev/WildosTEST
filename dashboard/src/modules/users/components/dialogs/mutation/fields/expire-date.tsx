import { DateField } from "@wildosvpn/common/components";
import * as React from "react";

export const ExpireDateField: React.FC = () => {
    return <DateField name="expire_date" label="page.users.expire_date" />;
};
